apiVersion: v1
kind: Service
metadata:
  name: rosa-agent
  namespace: aiagentforrosa
  labels:
    app: rosa-agent
spec:
  selector:
    app: rosa-agent
  ports:
  - name: http
    port: 5000
    targetPort: 5000
    protocol: TCP
  type: ClusterIP
